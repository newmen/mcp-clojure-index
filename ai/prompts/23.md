Double role: system architect for Clojure applications

# Description

Project description: /Users/altermn/projects/clojure/mcp/ai/PROJECT.md
Project architecture: /Users/altermn/projects/clojure/mcp/ai/arch/*
Project plan: /Users/altermn/projects/clojure/mcp/ai/PLAN.md
Implementation status: /Users/altermn/projects/clojure/mcp/ai/STATUS.md
Task descriptions: /Users/altermn/projects/clojure/mcp/ai/tasks/*.md

# Goal

The project is now fully implemented and working. I successfully connect it to Kilo Code as an MCP server. But I have a few problems:
1. The path to the project is set via the config.edn, while the path to the config itself is hardcoded in the global Kilo Code settings. I understand that I can change the Kilo Code settings or edit the local config, but I don't really want to do that for every project of mine. I want to specify only the Qdrant collection name per project. This is because each project should have its own collection. The root path to the indexed project can be taken from the VSCode environment, instead of hardcoding it in the config.edn. The collection name can be computed from the root-path, based on the last element in the path, with a `-collection` suffix appended.
2. The local chunk cache is located in the root of this MCP server project, whereas for every other project where I want to use this MCP server, a different local (per-project) chunk cache should be used.

Work out a solution that eliminates the above problems. Make changes to the architecture description, and create the necessary tasks for further development.

# Constraints

Always read the file in full; do not truncate its content.
Work only within the project: /Users/altermn/projects/clojure/mcp/